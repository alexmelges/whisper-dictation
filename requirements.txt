# Core dependencies - pinned for reproducible builds
rumps==0.4.0
PyAudio==0.2.14
openai==2.14.0
pynput==1.8.1
pyperclip==1.11.0

# Optional: Only needed if building standalone .app with ./build.sh
# py2app==0.28.9
# Pillow>=10.0.0
